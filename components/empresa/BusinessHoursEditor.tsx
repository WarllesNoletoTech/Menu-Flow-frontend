'use client';
import { useCallback, useEffect, useState } from 'react';
export type BusinessDay={dayOfWeek:number;isOpen:boolean;periods:Array<{openTime:string;closeTime:string}>};
type Response={configured:boolean;timezone:string;days:BusinessDay[]};const names=['Domingo','Segunda-feira','Terça-feira','Quarta-feira','Quinta-feira','Sexta-feira','Sábado'];const empty=():BusinessDay[]=>names.map((_,dayOfWeek)=>({dayOfWeek,isOpen:false,periods:[]}));
export function BusinessHoursEditor({request,endpoint}:{request:<T>(path:string,init?:RequestInit)=>Promise<T>;endpoint:string}){const[days,setDays]=useState<BusinessDay[]>(empty);const[timezone,setTimezone]=useState('America/Sao_Paulo');const[configured,setConfigured]=useState(false);const[busy,setBusy]=useState(false);const[message,setMessage]=useState('');const load=useCallback(async()=>{try{const result=await request<Response>(endpoint);setDays(result.configured?result.days:empty());setConfigured(result.configured);setTimezone(result.timezone);setMessage('')}catch(e){setMessage(e instanceof Error?e.message:'Não foi possível carregar os horários.')}},[endpoint,request]);useEffect(()=>{void load()},[load]);const change=(index:number,value:BusinessDay)=>setDays(items=>items.map((item,i)=>i===index?value:item));async function save(){if(busy)return;setBusy(true);setMessage('');try{await request(endpoint,{method:'PATCH',body:JSON.stringify({days})});setConfigured(true);setMessage('Horários de funcionamento atualizados com sucesso.')}catch(e){setMessage(e instanceof Error?e.message:'Não foi possível atualizar os horários.')}finally{setBusy(false)}}return <section className="mx-auto max-w-5xl"><header><h1 className="text-3xl font-black text-ink">Horários de funcionamento</h1><p className="mt-1 text-stone-500">Configure quando sua loja abre. Horários após meia-noite são aceitos.</p><p className="mt-1 text-xs font-bold text-stone-400">Fuso horário: {timezone}</p></header>{!configured&&<p className="mt-5 rounded-xl bg-amber-50 p-4 font-semibold text-amber-900">Horários ainda não configurados.</p>}<div className="mt-6 grid gap-4">{days.map((day,index)=><article key={day.dayOfWeek} className="rounded-2xl border border-border bg-surface p-4 shadow-sm sm:p-5"><div className="flex flex-wrap items-center justify-between gap-3"><h2 className="text-lg font-black">{names[day.dayOfWeek]}</h2><label className="flex min-h-11 cursor-pointer items-center gap-3 rounded-full bg-background px-4 font-bold"><input type="checkbox" checked={day.isOpen} onChange={e=>change(index,{...day,isOpen:e.target.checked,periods:e.target.checked?(day.periods.length?day.periods:[{openTime:'08:00',closeTime:'18:00'}]):[]})}/>{day.isOpen?'Aberto':'Fechado'}</label></div>{day.isOpen?<div className="mt-4 space-y-3">{day.periods.map((period,pIndex)=><div key={pIndex} className="grid items-end gap-2 sm:grid-cols-[1fr_auto_1fr_44px]"><label className="text-sm font-bold">Abertura<input aria-label={`Abertura de ${names[day.dayOfWeek]}`} type="time" className="field" value={period.openTime} onChange={e=>change(index,{...day,periods:day.periods.map((p,i)=>i===pIndex?{...p,openTime:e.target.value}:p)})}/></label><span className="hidden pb-3 text-stone-400 sm:block">até</span><label className="text-sm font-bold">Fechamento<input aria-label={`Fechamento de ${names[day.dayOfWeek]}`} type="time" className="field" value={period.closeTime} onChange={e=>change(index,{...day,periods:day.periods.map((p,i)=>i===pIndex?{...p,closeTime:e.target.value}:p)})}/></label><button type="button" disabled={day.periods.length===1} aria-label="Remover período" className="min-h-11 rounded-xl text-danger disabled:opacity-30" onClick={()=>change(index,{...day,periods:day.periods.filter((_,i)=>i!==pIndex)})}>✕</button></div>)}<button type="button" className="min-h-11 font-bold text-accent underline" onClick={()=>change(index,{...day,periods:[...day.periods,{openTime:'08:00',closeTime:'18:00'}]})}>+ Adicionar período</button></div>:<p className="mt-4 text-sm font-black text-stone-400">FECHADO</p>}</article>)}</div>{message&&<p role="status" className="mt-5 rounded-xl bg-surface p-4 font-semibold">{message}</p>}<button type="button" disabled={busy} onClick={()=>void save()} className="mt-6 w-full rounded-xl bg-ink px-6 py-3 font-black text-white disabled:opacity-60 sm:w-auto">{busy?'Salvando...':'Salvar horários'}</button></section>}
