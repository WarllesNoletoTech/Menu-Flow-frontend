'use client';
import Link from 'next/link'; import type { City } from '../../lib/public-restaurants'; import { UserMenu } from '../auth/UserMenu'; import { BrandLogo } from '../BrandLogo'; import { CitySelector } from './CitySelector'; import { SearchBar } from './SearchBar';
export function Header(props: { cities: City[]; city: string; onCityChange: (value: string) => void; search: string; onSearchChange: (value: string) => void }) {
  return <header className="border-b border-border bg-surface"><div className="mx-auto max-w-7xl px-4 py-5 sm:px-6"><div className="flex items-center justify-between gap-4"><Link href="/" className="flex items-center gap-3"><BrandLogo variant="wordmark" priority className="w-[clamp(8.5rem,22vw,12.5rem)] rounded-lg" /></Link><UserMenu /></div><div className="mt-5 grid gap-3 md:grid-cols-[minmax(220px,.65fr)_1.35fr]"><CitySelector cities={props.cities} value={props.city} onChange={props.onCityChange} /><SearchBar value={props.search} onChange={props.onSearchChange} /></div></div></header>;
}
