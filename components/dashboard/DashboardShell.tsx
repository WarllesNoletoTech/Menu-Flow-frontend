'use client';
import Link from 'next/link';import { ReactNode,useCallback,useEffect,useRef,useState } from 'react';import { usePathname,useRouter } from 'next/navigation';import type { Role } from '../../lib/auth';import { roleLabel } from '../../lib/auth';import { useAuth } from '../AuthProvider';import { DashboardProvider } from './DashboardContext';import { dashboardMenus,panelNames,type DashboardItem } from './config';
type Props={role:Role;children:ReactNode;establishment?:{name:string;slug?:string}|null;extraItems?:DashboardItem[]};
export function DashboardShell({role,children,establishment,extraItems=[]}:Props){const [mobile,setMobile]=useState(false);const close=useCallback(()=>setMobile(false),[]);return <DashboardProvider><div className="min-h-screen bg-stone-100 lg:pl-[270px]"><DashboardSidebar role={role} open={mobile} close={close} establishment={establishment} extraItems={extraItems}/><DashboardHeader role={role} open={mobile} toggle={()=>setMobile(v=>!v)} establishment={establishment}/><main className="p-4 sm:p-7">{children}</main></div></DashboardProvider>}
function DashboardSidebar({role,open,close,establishment,extraItems}:{role:Role;open:boolean;close:()=>void;establishment?:{name:string;slug?:string}|null;extraItems:DashboardItem[]}){const pathname=usePathname();const router=useRouter();const {logout}=useAuth();useEffect(()=>{const key=(e:KeyboardEvent)=>{if(e.key==='Escape')close()};document.addEventListener('keydown',key);return()=>document.removeEventListener('keydown',key)},[close]);const items=[...dashboardMenus[role],...extraItems];const active=(href:string)=>href==='/'?false:href===dashboardFor(role)?pathname===href:pathname.startsWith(href);return <><button type="button" aria-label="Fechar menu" className={`fixed inset-0 z-40 bg-black/50 lg:hidden ${open?'block':'hidden'}`} onClick={close}/><aside aria-label={`Navegação: ${panelNames[role]}`} className={`fixed inset-y-0 left-0 z-50 flex w-[270px] flex-col bg-ink px-4 py-6 text-white shadow-2xl transition-transform lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="px-3"><Link href={dashboardFor(role)} onClick={close} className="text-xl font-black"><span className="text-lime">MENU</span> FLOW</Link><p className="mt-5 truncate text-sm font-bold text-stone-300">{establishment?.name||panelNames[role]}</p></div><nav className="mt-6 flex-1 space-y-1 border-t border-white/10 pt-5">{items.map(item=><Link key={item.label} href={item.href} onClick={close} target={item.external?'_blank':undefined} aria-current={active(item.href)?'page':undefined} className={`flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-bold transition focus-visible:outline focus-visible:outline-2 focus-visible:outline-lime ${active(item.href)?'bg-lime text-ink':'text-stone-200 hover:bg-white/10'}`}><span className="w-5 text-center" aria-hidden>{item.icon}</span>{item.label}</Link>)}</nav><button type="button" onClick={()=>{logout();router.replace('/')}} className="flex items-center gap-3 border-t border-white/10 px-3 pt-5 text-sm font-bold text-stone-200 hover:text-lime"><span aria-hidden>⇥</span>Sair</button></aside></>}
function DashboardHeader({role,open,toggle,establishment}:{role:Role;open:boolean;toggle:()=>void;establishment?:{name:string;slug?:string}|null}){const pathname=usePathname();const router=useRouter();const {user,logout}=useAuth();const [menu,setMenu]=useState(false);const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const click=(e:MouseEvent)=>{if(!ref.current?.contains(e.target as Node))setMenu(false)};const key=(e:KeyboardEvent)=>{if(e.key==='Escape')setMenu(false)};document.addEventListener('mousedown',click);document.addEventListener('keydown',key);return()=>{document.removeEventListener('mousedown',click);document.removeEventListener('keydown',key)}},[]);if(!user)return null;const items=dashboardMenus[role];const title=items.filter(i=>!i.external&&(i.href===dashboardFor(role)?pathname===i.href:pathname.startsWith(i.href))).sort((a,b)=>b.href.length-a.href.length)[0]?.label||panelNames[role];const publicLabel=role==='SUPER_ADMIN'?'Portal público':role==='CUSTOMER'?'Explorar lojas':'Ver loja';const account=`${dashboardFor(role)}/conta`;return <header className="sticky top-0 z-30 flex min-h-20 items-center justify-between gap-3 border-b border-stone-200 bg-white/95 px-4 backdrop-blur sm:px-7"><div className="flex min-w-0 items-center gap-3"><button type="button" aria-label={open?'Fechar menu':'Abrir menu'} aria-expanded={open} onClick={toggle} className="grid h-11 w-11 place-items-center rounded-xl border lg:hidden">☰</button><div><p className="text-xs font-bold uppercase tracking-widest text-stone-400">{panelNames[role]}</p><h1 className="truncate text-xl font-black sm:text-2xl">{title}</h1></div></div><div className="relative" ref={ref}><button type="button" aria-haspopup="menu" aria-expanded={menu} onClick={()=>setMenu(!menu)} className="flex items-center gap-3 rounded-xl p-2 text-left hover:bg-stone-100"><span className="grid h-10 w-10 place-items-center rounded-full bg-ink font-black text-lime">{user.name.charAt(0).toUpperCase()}</span><span className="hidden max-w-56 sm:block"><strong className="block truncate text-sm">{user.name}</strong><small className="block truncate text-stone-500">{roleLabel[user.role]}{establishment&&` · ${establishment.name}`}</small></span><span aria-hidden>⌄</span></button>{menu&&<div role="menu" className="absolute right-0 mt-2 w-56 rounded-2xl border bg-white p-2 shadow-soft"><Link role="menuitem" href={account} onClick={()=>setMenu(false)} className="block rounded-xl px-4 py-3 text-sm font-bold hover:bg-stone-100">Minha conta</Link><Link role="menuitem" href={establishment?.slug?`/${establishment.slug}`:'/'} onClick={()=>setMenu(false)} className="block rounded-xl px-4 py-3 text-sm font-bold hover:bg-stone-100">{publicLabel}</Link><button type="button" role="menuitem" onClick={()=>{logout();router.replace('/')}} className="w-full rounded-xl px-4 py-3 text-left text-sm font-bold text-red-700 hover:bg-red-50">Sair</button></div>}</div></header>}
function dashboardFor(role:Role){return role==='SUPER_ADMIN'?'/admin':role==='EMPLOYEE'?'/funcionario':role==='CUSTOMER'?'/cliente':'/empresa'}
