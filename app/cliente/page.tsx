'use client';
import Link from 'next/link';import { useAuth } from '../../components/AuthProvider';
export default function Page(){const {user}=useAuth();return <section className="mx-auto max-w-5xl"><div className="rounded-3xl bg-ink p-7 text-white"><p className="text-sm font-bold text-lime">BEM-VINDO AO MENU FLOW</p><h2 className="mt-2 text-2xl font-black">Olá, {user?.name.split(' ')[0]}!</h2><p className="mt-2 text-stone-300">Acompanhe seus pedidos e mantenha seus dados atualizados.</p></div><div className="mt-6 grid gap-4 sm:grid-cols-3">{[['Meus pedidos','/cliente/pedidos'],['Meus endereços','/cliente/enderecos'],['Explorar estabelecimentos','/']].map(([label,href])=><Link key={href} href={href} className="rounded-2xl bg-white p-6 font-bold shadow-sm hover:-translate-y-0.5">{label}<span className="float-right">→</span></Link>)}</div></section>}
