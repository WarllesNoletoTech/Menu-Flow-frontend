'use client';import { OrdersList } from '../../../components/dashboard/OrdersList';import { useEmpresa } from '../../../components/empresa/EmpresaContext';export default function Page(){const {establishment}=useEmpresa();return <section className="mx-auto max-w-7xl"><h2 className="text-2xl font-black">Pedidos</h2><p className="mt-1 text-stone-500">Painel operacional dos pedidos da sua loja.</p>{establishment&&<OrdersList restaurantId={establishment._id}/>}</section>}
