'use client';
import { useEffect,useState } from 'react';import { useDashboard } from '../../components/dashboard/DashboardContext';
type Data={total:number;active:number;blocked:number;types:Record<string,number>};const labels:Record<string,string>={RESTAURANT:'Restaurantes',PHARMACY:'Farmácias',CLOTHING:'Lojas de roupas',OTHER:'Outros estabelecimentos'};
export default function AdminPage(){const {request}=useDashboard();const [data,setData]=useState<Data|null>(null);const [error,setError]=useState('');useEffect(()=>{request<Data>('/users/dashboard').then(setData).catch(e=>setError(e.message))},[request]);return <section className="mx-auto max-w-6xl"><div className="rounded-3xl bg-ink p-7 text-white"><p className="text-sm font-bold text-lime">MENU FLOW</p><h2 className="mt-2 text-2xl font-black">Visão geral da plataforma</h2><p className="mt-2 text-stone-300">Informações consolidadas e reais dos estabelecimentos.</p></div>{error&&<p role="alert" className="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{error}</p>}<div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{data&&[['Total de estabelecimentos',data.total],['Estabelecimentos ativos',data.active],['Estabelecimentos bloqueados',data.blocked],...Object.entries(labels).map(([key,label])=>[label,data.types[key]||0])].map(([label,value])=><article key={label} className="rounded-2xl bg-white p-5 shadow-sm"><p className="text-sm font-bold text-stone-500">{label}</p><strong className="mt-3 block text-3xl font-black">{value}</strong></article>)}</div></section>}
